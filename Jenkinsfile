pipeline {
     agent any
     stages {
          stage("Checkout") {
               steps {
                    git url: 'https://github.com/brundaged/calculator.git', branch: 'main'
               }
          }
     }
}
